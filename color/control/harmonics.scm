#!/usr/bin/env gosh
;; -*- mode: gauche; coding: utf-8; -*-

(define-module color.control.harmonics
  (extend color.control.harmonics.hue-contrast)
  (export-all))

(select-module color.control.harmonics)

(provide "color/control/harmonics")
;; EOF